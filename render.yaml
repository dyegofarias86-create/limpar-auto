services:
  - type: web
    name: limpar-auto
    env: node
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd backend && npm install && NODE_ENV=production node server.js
    envVars:
      - key: NODE_ENV
        value: production
